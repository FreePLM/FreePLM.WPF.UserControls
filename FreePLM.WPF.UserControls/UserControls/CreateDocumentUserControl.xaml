<UserControl x:Class="FreePLM.WPF.UserControls.UserControls.CreateDocumentUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:FreePLM.WPF.UserControls.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="500">
    <UserControl.DataContext>
        <viewmodels:CreateDocumentViewModel/>
    </UserControl.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"
                   Text="Create New PLM Document"
                   FontSize="18" FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <!-- Object ID (read-only) -->
        <TextBlock Grid.Row="1" Grid.Column="0" Text="Object ID:"
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="1" Grid.Column="1"
                 Text="{Binding ObjectId, Mode=OneWay}"
                 IsReadOnly="True" Background="#F0F0F0"
                 Margin="0,0,0,10"/>

        <!-- Revision (read-only) -->
        <TextBlock Grid.Row="2" Grid.Column="0" Text="Initial Revision:"
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding Revision, Mode=OneWay}"
                 IsReadOnly="True" Background="#F0F0F0"
                 Margin="0,0,0,10"/>

        <!-- File Name -->
        <TextBlock Grid.Row="3" Grid.Column="0" Text="File Name:*"
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="3" Grid.Column="1"
                 Text="{Binding FileName, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,10"/>

        <!-- Project -->
        <TextBlock Grid.Row="4" Grid.Column="0" Text="Project:*"
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="4" Grid.Column="1"
                 Text="{Binding Project, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,10"/>

        <!-- Owner -->
        <TextBlock Grid.Row="5" Grid.Column="0" Text="Owner:*"
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="5" Grid.Column="1"
                 Text="{Binding Owner, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,10"/>

        <!-- Group -->
        <TextBlock Grid.Row="6" Grid.Column="0" Text="Group:*"
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="6" Grid.Column="1"
                 Text="{Binding Group, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,10"/>

        <!-- Role -->
        <TextBlock Grid.Row="7" Grid.Column="0" Text="Role:*"
                   VerticalAlignment="Center" Margin="0,0,10,10"/>
        <TextBox Grid.Row="7" Grid.Column="1"
                 Text="{Binding Role, UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,0,10"/>

        <!-- Comment -->
        <TextBlock Grid.Row="8" Grid.Column="0" Text="Comment:"
                   VerticalAlignment="Top" Margin="0,5,10,10"/>
        <TextBox Grid.Row="8" Grid.Column="1"
                 Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap" AcceptsReturn="True"
                 Height="60" Margin="0,0,0,10"/>

        <!-- Validation Error -->
        <TextBlock Grid.Row="9" Grid.Column="0" Grid.ColumnSpan="2"
                   Text="{Binding ValidationError}"
                   Foreground="Red" TextWrapping="Wrap"
                   Margin="0,0,0,10"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="11" Grid.Column="0" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Create" Width="80" Height="30"
                    Command="{Binding CreateCommand}"
                    IsDefault="True" Margin="0,0,10,0"/>
            <Button Content="Cancel" Width="80" Height="30"
                    Command="{Binding CancelCommand}"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</UserControl>
